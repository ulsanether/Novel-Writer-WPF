# AI 삽화 생성 기능 설계 (검토용)

> 씬(Scene)별로 소설 삽화를 생성하는 기능. **삽화 일관성**과 **이미지 생성 AI 별도 관리**, **검열/무검열 표시**가 핵심 요구사항입니다.
> 이 문서는 검토용 초안입니다. 승인 후 구현합니다.

---

## 1. 목표

- 스토리 플래너의 **각 Scene에 삽화**를 붙인다. (씬 편집 화면에서 생성·미리보기·저장)
- **일관성**: 같은 인물·같은 화풍이 소설 전체에서 유지되도록 한다.
- 이미지 생성 AI를 **텍스트 LLM(Ollama)과 분리**해 별도로 관리한다.
- 이미지 모델의 **검열/무검열 여부를 표시**한다. (텍스트 모델 배지와 동일 컨셉)

---

## 2. 이미지 생성 백엔드 (별도 관리)

텍스트는 Ollama(LLM), 이미지는 **별도 이미지 생성 서버**를 사용합니다. Ollama는 이미지를 생성하지 못합니다.

| 백엔드 | 방식 | 장점 | 단점 | 검열 |
|--------|------|------|------|------|
| **AUTOMATIC1111 (SD WebUI)** ⭐ | 로컬 · `--api` (`/sdapi/v1/txt2img`) | 무료, 커스텀 모델/LoRA/ControlNet, 일관성 도구 풍부 | VRAM 필요(6~8GB+), 설치 필요 | 사용자 선택 (safety checker on/off) |
| **ComfyUI** | 로컬 · API(워크플로) | 고급 파이프라인, IP-Adapter 등 | 설정 복잡 | 사용자 선택 |
| **Fooocus** | 로컬 · API | 쉬운 UI, 품질 좋음 | API 제한적 | 사용자 선택 |
| **Stability AI API** | 클라우드 · REST | 설치 불필요, SDXL/SD3 | 유료, 키 필요 | 중간(정책) |
| **OpenAI (DALL·E 3 / gpt-image)** | 클라우드 · REST | 품질·프롬프트 이해 좋음 | 유료, **검열 강함** | 강함 |
| **Replicate** | 클라우드 · REST | 다양한 모델 | 유료 | 모델별 |

**권장 기본값**: **AUTOMATIC1111 SD WebUI (로컬)** — 삽화 일관성 도구(LoRA·ControlNet·IP-Adapter)와 커스텀 화풍 모델을 자유롭게 쓸 수 있어 소설 삽화에 가장 적합합니다. 설치 없이 쓰려면 Stability/OpenAI API 옵션도 지원.

> AI 오타·스토리와 마찬가지로 **환경변수 > 앱 설정 > 기본값** 우선순위, **로컬 주소는 키 없이 호출** 규칙을 그대로 적용합니다.

---

## 3. 삽화 일관성 (가장 중요)

한 소설 안에서 인물·화풍이 흔들리지 않게 하는 장치들:

### 3-1. 화풍(스타일) 일관성
- **스타일 프리픽스**: 프로젝트 단위의 공통 프롬프트(예: `"수채화 삽화, 부드러운 조명, 따뜻한 색조, 동화풍"`)를 모든 이미지 앞에 자동으로 붙임.
- **고정 체크포인트/샘플러/스텝/CFG**: 프로젝트 내내 동일한 모델·설정 사용.
- **네거티브 프롬프트** 공통 관리(원치 않는 요소 배제).

### 3-2. 캐릭터 일관성 (핵심 난제)
소설 삽화에서 가장 어려운 부분. **캐릭터 설정집(참고자료) → 레퍼런스 시트 → 씬 삽화** 순서로 만들어 고정하는 것이 핵심입니다(아래 3-4). 사용하는 기법:

1. **참고자료 생성기 캐릭터 활용** (기본·필수)
   - **참고자료 생성기로 만든 캐릭터 설정집(`Characters/*.md`)** 을 캐릭터 소스로 사용.
   - 설정집에서 **외모·복장**(머리색·눈·체형·나이·의상 등)을 추출해 캐릭터의 **외형 프롬프트**로 저장.
   - 그 인물이 등장하는 씬 프롬프트에 외형 프롬프트를 자동 삽입.
2. **시드 고정**
   - 캐릭터/씬별 seed를 저장·재사용해 재생성 시 유사한 결과.
3. **레퍼런스 이미지(캐릭터 시트) + IP-Adapter** (중급, 권장)
   - **캐릭터 레퍼런스 시트**(전/후/좌/우 360도 외형·복장, 아래 3-4)를 먼저 만들고, 이를 IP-Adapter/레퍼런스로 등록 → 이후 모든 씬이 같은 얼굴·복장을 유지.
4. **캐릭터 LoRA** (고급)
   - 레퍼런스 시트 이미지들로 캐릭터 전용 LoRA를 학습 → `<lora:name>` 삽입. 가장 일관적이나 학습 필요.

> **구현 단계 제안**: 1단계 **참고자료 캐릭터 → 외형 프롬프트 + 시드 고정 + 레퍼런스 시트 생성**(설치 최소)으로 시작하고, IP-Adapter → LoRA로 확장.

### 3-3. 씬 → 이미지 프롬프트 자동 생성
- 씬 정보(장소·인물·분위기·사건) + 등장인물 외형 + 스타일 프리픽스를 **LLM(Ollama)이 영어 이미지 프롬프트로 변환**. (SD 모델은 영어 프롬프트가 품질이 좋음)
- 생성된 프롬프트는 **사용자가 편집 가능**(재생성 전 수정).

### 3-4. 캐릭터 제작 파이프라인 (권장 · "설정집 → 시트 → 삽화")
캐릭터를 씬에 바로 그리지 않고, **먼저 캐릭터를 확정**한 뒤 그 캐릭터를 레퍼런스로 재사용합니다.

```
① 캐릭터 설정집 (참고자료 생성기)
      ↓  외모·복장 추출
② 캐릭터 레퍼런스 시트 생성  ← 전/후/좌/우 360° + 전신·표정·복장 변형
      ↓  IP-Adapter / 외형 프롬프트 / (LoRA)로 등록
③ 씬 삽화 생성  ← 등장인물의 레퍼런스를 자동 참조하여 일관되게
```

1. **① 캐릭터 설정집** — 이미 있는 **참고자료 생성기**로 캐릭터(.md)를 만든다. 외모·복장 항목을 포함(없으면 "외모·복장 보강" 생성).
2. **② 캐릭터 레퍼런스 시트(턴어라운드)** — 설정집을 기반으로 한 캐릭터의 **전신 전/후/좌/우 4방향(+선택: 3/4 각도, 표정 시트, 복장 변형)** 이미지를 먼저 생성한다.
   - 프롬프트에 `character sheet, turnaround, front/back/side view, full body, consistent character, neutral background` 등 캐릭터 시트 지시를 사용.
   - 방향별로 **같은 seed / 외형 프롬프트**를 공유해 동일 인물이 되게 함.
   - 마음에 드는 시트가 나올 때까지 재생성 → **확정(캐릭터 대표 레퍼런스로 저장)**.
3. **③ 씬 삽화** — 각 씬에서 등장인물의 **확정 레퍼런스**(IP-Adapter 입력 또는 외형 프롬프트)를 자동으로 참조 → 인물이 소설 내내 일관되게 그려짐.

> 즉 **"캐릭터 설정집 먼저 → 360° 외형/복장 시트 먼저 확정 → 이후 삽화는 그 시트를 기반"** 으로 관리합니다. 이렇게 하면 씬마다 얼굴·복장이 바뀌는 문제를 크게 줄입니다.

### 3-5. 배경(장소) 일관성 파이프라인
캐릭터와 동일한 원리를 **장소**에도 적용합니다. 같은 장소가 씬마다 다르게 그려지는 것을 막습니다.

```
① 장소 설정집 (참고자료 생성기: 세계관/장소·배경 설정)  → Backgrounds/*.md
      ↓  배경 묘사 추출
② 장소 레퍼런스 이미지 생성  ← 대표 전경(establishing shot) + 여러 각도/시간대(낮·밤)·계절
      ↓  장소 프롬프트 / 레퍼런스로 등록
③ 씬 삽화 생성  ← 씬의 장소(Location)에 맞는 레퍼런스를 자동 참조하여 배경 일관
```

- 씬의 `location` 값과 **장소 설정집(Backgrounds/*.md)** 을 매칭 → 그 장소의 배경 프롬프트/레퍼런스를 씬 삽화에 자동 삽입.
- 장소별 **고정 시드 + 배경 프롬프트**로 톤·구조를 유지.
- (선택) 대표 전경을 **IP-Adapter/ControlNet** 참조로 사용해 구도까지 고정.

> 결과적으로 **인물(3-4) + 배경(3-5)** 이 각각 레퍼런스로 고정되어, 씬 삽화는 "확정된 인물 × 확정된 장소 × 공통 화풍"으로 일관되게 생성됩니다.

### 3-6. 자동 생성 워크플로우 (참고자료 생성기에서 시작)
사용자가 하나씩 만들 수도 있지만, **참고자료 생성기에서 시작해 일관된 인물·배경·삽화까지 한 번에** 뽑는 자동 흐름을 제공합니다.

```
[참고자료 생성기 · 자동 생성]
   ↓ (작품 설정/시놉시스 기반)
① 캐릭터 설정집들 + 장소 설정집들 자동 생성 → Characters/*.md, Backgrounds/*.md
   ↓
② 캐릭터 레퍼런스 시트(360°) + 장소 레퍼런스(전경) 자동 생성·저장
   ↓
③ 씬별 삽화 자동 생성  ← 등장인물 시트 × 장소 레퍼런스 × 공통 화풍
```

- **[전체 자동] 버튼**: 위 ①→②→③을 순차 실행(각 단계 진행률 표시, 로컬 모델이라 시간 소요).
- **공통 화풍 고정**: 모든 이미지에 프로젝트 스타일 프리픽스·네거티브·동일 체크포인트 적용 → 자동 생성이어도 화풍 일관.
- **단계별 개별 실행**도 가능(캐릭터만/배경만/특정 씬만).
- **중간 확정 개입**: 자동 생성 후 마음에 안 드는 캐릭터 시트·장소 레퍼런스는 재생성·확정 → 이후 씬 삽화에 반영.
- **덮어쓰기 확인**: 기존 설정집/레퍼런스가 있으면 확인 후 진행(스토리 역분석과 동일 방식).

> 텍스트(설정집·프롬프트)는 **Ollama(LLM)**, 이미지는 **이미지 백엔드(SD 등)** 가 담당하며, 자동 워크플로우가 둘을 순서대로 호출합니다.

---

## 4. 이미지 생성 AI 별도 관리 + 검열/무검열 표시

### 4-1. 별도 설정 창 (텍스트 LLM과 분리)
"이미지 생성" 전용 설정(또는 창):
- 백엔드 종류(WebUI/ComfyUI/Stability/OpenAI) + 서버 주소 + API 키
- **체크포인트(이미지 모델)** 선택 (설치된 모델 목록 조회)
- 샘플러 / 스텝 / CFG / 해상도 / 배치
- 스타일 프리픽스 / 공통 네거티브 프롬프트
- **안전 필터(safety checker) on/off**

### 4-2. 검열/무검열 배지 (텍스트 모델 배지와 동일 컨셉)
이미지 모델 목록에 배지 표시:
- 🟢 **검열(SFW)**: 안전 필터 on 또는 SFW 지향 모델
- 🟠 **무검열(NSFW 허용)**: 안전 필터 off 또는 NSFW 허용 모델(모델명 키워드로 추정)
- `ModelBadgeKeywords`와 유사하게 이미지 모델용 키워드 세트 추가(예: `nsfw`, `uncensored`, `hentai`, `pony`, `realistic-vision` 등은 상황에 따라).

> **주의(콘텐츠 책임)**: 무검열 이미지 생성은 성인·유해 콘텐츠 생성이 가능합니다. **개인 창작 용도**이며 생성물·배포의 책임은 사용자에게 있습니다. 앱은 옵션과 표시만 제공합니다.

---

## 5. 데이터 모델 (원고/설계와 분리)

`SceneNode`에 삽화 필드 추가:
```json
{
  "illustrationPath": "",     // 저장된 이미지 파일 경로
  "illustrationPrompt": "",   // 이미지 생성 프롬프트(편집 가능)
  "illustrationSeed": -1      // 시드(고정/재사용)
}
```
`StoryCharacter`(등장인물)에 이미지 관련 필드 추가 — 참고자료 캐릭터와 레퍼런스 시트를 연결:
```json
{
  "sourceReference": "Characters/주인공.md",  // 참고자료 생성기로 만든 설정집 경로
  "appearancePrompt": "",                     // 외모·복장 프롬프트(설정집에서 추출)
  "referenceSeed": -1,                        // 캐릭터 고정 시드
  "referenceSheet": {                         // 확정된 360° 레퍼런스 이미지 경로
    "front": "", "back": "", "left": "", "right": "",
    "extra": []                               // 3/4각도·표정·복장 변형 등
  }
}
```
**장소(배경) 관리** — `Backgrounds/*.md` 설정집과 레퍼런스를 연결:
```json
{
  "name": "왕궁의 스토리룸",
  "sourceReference": "Backgrounds/왕궁.md",   // 참고자료 생성기 설정집
  "backgroundPrompt": "",                      // 배경 프롬프트(설정집에서 추출)
  "referenceSeed": -1,
  "referenceImages": { "wide": "", "extra": [] }  // 대표 전경 + 각도/시간대 변형
}
```
`StoryProject`(또는 별도 설정)에:
```json
{
  "imageStylePrefix": "",     // 화풍 프리픽스(전체 공통)
  "imageNegative": "",        // 공통 네거티브
  "locations": []             // 장소 목록(위 배경 구조)
}
```
`SceneNode`에:
```json
{
  "illustrationPath": "", "illustrationPrompt": "", "illustrationSeed": -1
}
```
- **이미지 파일 저장 위치**: 참고자료 폴더의 `Illustrations/` (씬 삽화) 및 `Characters/Sheets/<인물>/` (레퍼런스 시트), 또는 `%LocalAppData%/NovelWriter/Illustrations/`.
- JSON에는 **경로만** 저장(이미지 바이너리는 파일로).

---

## 6. UI 흐름

**스토리 플래너 · 등장인물 편집**에 "캐릭터 이미지" 구역:
```
[설정집에서 외모·복장 추출]  ← Characters/*.md → 외형 프롬프트
[외형 프롬프트 편집]  [고정 시드]
[레퍼런스 시트 생성: 전/후/좌/우 360°]  [재생성]
[시트 미리보기(4방향)]  [확정 → 대표 레퍼런스 저장]
```

**스토리 플래너 · Scene 편집 패널** 하단에 "삽화" 구역:
```
[삽화 프롬프트 생성(AI)]  ← 씬+등장인물 확정 레퍼런스+스타일 → 이미지 프롬프트
[프롬프트 편집 TextBox]
[이미지 생성]  [시드 고정 ☑]  [재생성]
[생성된 이미지 미리보기]  [저장]  [에디터에 삽입(선택)]
```
- 생성 진행률/상태 표시.
- 이미지 백엔드 미설정/미실행 시 안내(오버레이 or 상태 메시지).

---

## 7. 구현 단계 (Phase)

1. **Phase 1 — 기본 파이프라인**
   - `ImageGenService`(AUTOMATIC1111 `txt2img` 우선) + 설정(백엔드/모델/샘플러 등).
   - 씬 → 이미지 프롬프트 생성(LLM) → 생성 → 미리보기 → 파일 저장.
   - `SceneNode`에 삽화 필드, `Illustrations/` 폴더.
2. **Phase 2 — 캐릭터·배경 파이프라인 & 일관성**
   - **참고자료 캐릭터 → 외모·복장 추출** → **캐릭터 레퍼런스 시트(360°) 생성·확정** → `Characters/Sheets/`.
   - **참고자료 장소 → 배경 추출 → 장소 레퍼런스(전경) 생성·확정** → 씬의 `location` 매칭.
   - 스타일 프리픽스·공통 네거티브, 시드 고정/재사용. 씬 삽화가 인물·장소 레퍼런스를 **자동 참조**.
3. **Phase 3 — 자동 생성 워크플로우**
   - 참고자료 생성기 **[전체 자동]**: 캐릭터·장소 설정집 → 레퍼런스 → 씬 삽화 일괄(진행률·덮어쓰기 확인).
4. **Phase 4 — 이미지 모델 관리 + 검열 배지**
   - 이미지 모델 별도 설정 창, 설치 모델 목록 조회, 검열/무검열 배지.
5. **Phase 5(선택) — 고급 일관성**
   - IP-Adapter(레퍼런스 이미지)·ControlNet(구도)·캐릭터 LoRA 지원.

---

## 8. 검토가 필요한 결정 사항

1. **기본 백엔드**: AUTOMATIC1111(로컬) 우선이 맞는지? (설치 필요) 아니면 클라우드 API(Stability/OpenAI)부터?
2. **캐릭터 일관성 1단계**: "외형 프롬프트 + 시드 고정"으로 시작 → 이후 IP-Adapter/LoRA 확장, 동의하시는지?
3. **이미지 저장 위치**: 참고자료 폴더의 `Illustrations/` vs `%LocalAppData%/NovelWriter/Illustrations/`.
4. **검열 배지 판정**: 모델명 키워드 기반(자동 추정) + 안전 필터 on/off 표시, 이 방식으로 충분한지?
5. **프롬프트 언어**: 이미지 프롬프트를 영어로 생성(SD 품질↑)하되 UI는 한국어 — 괜찮은지?
6. **캐릭터 레퍼런스 시트 구성**: 전/후/좌/우 4방향 기본 + (선택) 3/4 각도·표정·복장 변형 — 이 구성으로 충분한지?
7. **레퍼런스 반영 방식**: 1단계는 **외형 프롬프트 + 시드 고정**(설치 불필요), 2단계 **IP-Adapter**(레퍼런스 이미지 직접 참조, 확장 설치 필요)로 확장 — 이 순서에 동의하시는지?
8. **배경 일관성**: 캐릭터와 동일하게 **장소도 레퍼런스(전경)를 먼저 만들고** 씬에서 재사용하는 방식 — 동의하시는지? 장소 레퍼런스는 전경 1장 + 시간대(낮·밤) 변형 정도면 충분한지?
9. **자동 생성 범위**: 참고자료 생성기 **[전체 자동]**(설정집→레퍼런스→씬 삽화 일괄)을 제공할지, 아니면 **단계별 버튼만** 둘지? (전체 자동은 이미지 수가 많아 시간·디스크 사용이 큼)

> 위 9개를 확인해 주시고 "시작"이라고 하시면 Phase 1부터 구현하겠습니다.
