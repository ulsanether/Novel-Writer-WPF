<Window
    x:Class="NovelWriter.Wpf.FirstRunSetupWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:NovelWriter.Wpf"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="처음 설치 마법사"
    Width="640"
    Height="760"
    WindowStartupLocation="CenterScreen"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:InverseBoolConverter x:Key="InverseBool" />
    </Window.Resources>

    <DockPanel Margin="0">
        <!-- 하단 -->
        <Border DockPanel.Dock="Bottom" Padding="24,12" Background="#22808080">
            <DockPanel>
                <ProgressBar
                    Width="24"
                    Height="24"
                    VerticalAlignment="Center"
                    IsIndeterminate="True"
                    Style="{StaticResource MaterialDesignCircularProgressBar}"
                    Value="0"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button
                        Margin="0,0,8,0"
                        Command="{Binding SetupAllCommand}"
                        Content="전체 자동 설치"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
                    <Button
                        Margin="0,0,8,0"
                        Command="{Binding FinishCommand}"
                        Content="완료"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
                    <Button
                        Command="{Binding SkipCommand}"
                        Content="나중에"
                        Style="{StaticResource MaterialDesignOutlinedButton}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <TextBlock Margin="0,0,0,4" FontSize="20" FontWeight="Bold" Text="🚀 처음 설치 마법사" />
                <TextBlock Margin="0,0,0,16" Opacity="0.75" Text="글쓰기 AI(텍스트)와 삽화 AI(이미지)를 자동으로 설치·준비합니다. 각각 따로 하거나, 아래 [전체 자동 설치]로 한 번에 진행할 수 있습니다. (수 GB 다운로드 · 시간 소요)" TextWrapping="Wrap" />

                <!-- 텍스트 AI -->
                <Border Margin="0,0,0,14" Padding="14" Background="#18808080" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Text="① 텍스트 AI — Ollama + 소설 모델" />
                        <TextBlock Margin="0,0,0,8" FontSize="12" Opacity="0.7" Text="오타 보정·스토리 플래너·참고자료 생성에 쓰입니다. Ollama를 자동 설치(winget)하고 아래 모델을 받습니다." TextWrapping="Wrap" />
                        <TextBlock Margin="0,0,0,2" FontSize="12" Text="사용할 모델" />
                        <ComboBox Margin="0,0,0,8" IsEditable="True" ItemsSource="{Binding LlmModels}" Style="{StaticResource MaterialDesignOutlinedComboBox}" Text="{Binding SelectedLlm}" />
                        <StackPanel Orientation="Horizontal">
                            <Button Margin="0,0,8,0" Command="{Binding SetupTextAiCommand}" Content="텍스트 AI 설치" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock VerticalAlignment="Center" FontSize="12" Text="{Binding OllamaStatus}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 이미지 AI -->
                <Border Margin="0,0,0,14" Padding="14" Background="#18808080" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Text="② 이미지 AI — ComfyUI + 이미지 모델" />
                        <TextBlock Margin="0,0,0,8" FontSize="12" Opacity="0.7" Text="캐릭터·장면 삽화 생성에 쓰입니다. ComfyUI 포터블(Python 불필요)을 받고 이미지 모델을 내려받습니다." TextWrapping="Wrap" />
                        <TextBlock Margin="0,0,0,2" FontSize="12" Text="설치 폴더" />
                        <DockPanel Margin="0,0,0,8">
                            <Button Margin="8,0,0,0" Command="{Binding ChooseFolderCommand}" Content="폴더" DockPanel.Dock="Right" Style="{StaticResource MaterialDesignOutlinedButton}" />
                            <TextBox IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding ComfyInstallDir}" />
                        </DockPanel>
                        <TextBlock Margin="0,0,0,2" FontSize="12" Text="이미지 모델" />
                        <ComboBox Margin="0,0,0,4" DisplayMemberPath="DisplayName" ItemsSource="{Binding ComfyModels}" SelectedItem="{Binding SelectedComfyModel}" Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                        <TextBlock Margin="0,0,0,8" FontSize="11" Opacity="0.7" Text="{Binding SelectedComfyModel.Note}" TextWrapping="Wrap" />
                        <StackPanel Orientation="Horizontal">
                            <Button Margin="0,0,8,0" Command="{Binding SetupImageAiCommand}" Content="이미지 AI 설치" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock VerticalAlignment="Center" FontSize="12" Text="{Binding ComfyStatus}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <TextBlock Margin="0,0,0,4" FontWeight="SemiBold" Text="진행 로그" />
                <Border Padding="8" Background="#22000000" CornerRadius="4">
                    <TextBox Height="150" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="Transparent" BorderThickness="0" FontFamily="Consolas" FontSize="11" IsReadOnly="True" Text="{Binding Log}" TextWrapping="NoWrap" />
                </Border>
                <TextBlock Margin="0,8,0,0" FontSize="11" Opacity="0.6" Text="※ Ollama·ComfyUI 설치 시 winget 관리자 권한 창이 뜰 수 있습니다. 나중에 설정 창에서도 설치할 수 있습니다." TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
